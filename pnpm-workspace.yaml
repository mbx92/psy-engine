# Single-package workspace + pnpm install policy
packages:
  - '.'

# Nuxt/Vite need esbuild binaries via install scripts
allowBuilds:
  esbuild: true
  vue-demi: true

minimumReleaseAgeExclude:
  - '@lucide/vue@1.27.0'
